{
  "name": "@chef/runtime",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=24.0.0" },
  "scripts": {
    "start": "node --experimental-strip-types src/main.ts",
    "test": "node --experimental-strip-types tests/golden-path.ts",
    "typecheck": "node --experimental-strip-types --experimental-transform-types src/main.ts --check 2>&1 || true"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.62.0",
    "node-pty": "^1.1.0"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "typescript": "^7.0.2"
  }
}
