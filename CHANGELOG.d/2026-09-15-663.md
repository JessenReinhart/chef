# PR #663

Chef now only shows a `Run:` instruction when the completed result also has a usable location that you can reveal. This avoids telling you how to run generated work without showing where that work actually is.
