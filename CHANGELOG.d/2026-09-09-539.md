# PR #539

Chef now keeps Thread selection and task submission working when the browser blocks local storage. The selected Thread stays usable for the current session, and Simple Mode protections remain active instead of crashing or silently turning off.
