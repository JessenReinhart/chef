# PR #548

Chef now keeps a verification failure visible instead of replacing it with a misleading “still verifying” heartbeat. Progress feedback resumes only after the failed task or Mission genuinely starts recovering.
