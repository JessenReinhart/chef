# Completion report shows where your result is

- Chef's final Simple Mode completion message now includes the generated result location and run command when the worker provides them.
- Local `file:` locations are shown as readable paths instead of raw encoded URIs, including the existing artifact-URI fallback behavior.

PR #600
