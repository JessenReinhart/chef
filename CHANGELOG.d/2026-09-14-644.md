# PR #644

- Simple Mode now keeps a usable result location visible even when newer Mission outputs fill the result cards.
- Runnable commands stay visible too, so users can still find the generated app and run it without digging through older output.
