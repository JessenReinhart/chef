# PR #632

- When Chef cannot reopen a selected project after retrying, Simple Mode now keeps the useful runtime failure reason so users can see what actually went wrong.
- Temporary restart errors that recover no longer hide a later, genuine wrong-project result.
