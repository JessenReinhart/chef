# PR #657

Chef now recognizes Windows extended-length project paths as the same project you selected. This prevents a project from opening successfully but then being reported as not selected just because Windows returned the path in a different valid format.
