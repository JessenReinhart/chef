### Fixed

- Retrying a failed Mission now reconnects that work to the same Mission and plan, so Simple Mode shows it working again and then verifying/completing instead of leaving the retry hidden underneath failed history.
