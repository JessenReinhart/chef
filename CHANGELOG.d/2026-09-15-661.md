# PR #661

Chef no longer treats the project folder itself as a finished result when a worker reports placeholder paths such as `.`. Completed work now waits for a concrete result location, while normal child paths such as `dist/todo-app` still work as expected.
