# PR #497

Simple Mode now fully unlocks a Thread as soon as an accepted Mission appears in the live Mission list, so an immediate follow-up no longer gets rejected by a stale “already starting” guard.
