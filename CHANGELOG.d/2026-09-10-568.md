# Thread submissions stay with the selected Thread

- Switching Simple Mode Threads now immediately releases the previous Thread's in-flight send state.
- Late success or error responses from an older Thread can no longer appear in the newly selected conversation or unlock a newer send.

PR #568
