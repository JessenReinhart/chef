# Thread chat stays with the selected Thread

- Switching Simple Mode Threads now clears the old conversation immediately and loads the newly selected Thread's history.
- Slow history responses from a previous Thread can no longer overwrite the conversation you just switched to.

PR #566