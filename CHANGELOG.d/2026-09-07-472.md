# PR #472

After Chef accepts a Mission, Simple Mode now keeps the visible starting handoff through a remount or Thread switch until that exact Mission appears in authoritative state, reducing confusing duplicate-submission windows.
