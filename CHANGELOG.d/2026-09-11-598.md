# Keep verification summaries truthful

- Chef no longer shows mixed pass/fail verification summaries under a positive **Verified** result.
- Clear zero-failure summaries such as `No tests failed` and `5 tests passed, 0 failed` still remain visible.
- This keeps completion feedback aligned with what the worker actually verified instead of making partial failures look successful.
