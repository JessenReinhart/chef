- **Chef now permanently checks the full todo-app completion handoff (#604):** The Windows and Linux golden path verifies that the final Simple Mode message tells you what changed, where the generated app is, how to run it, and what was verified, while still proving the app actually runs.
