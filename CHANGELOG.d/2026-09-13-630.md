# PR #630

- Simple Mode now says **Show location** when it opens or selects a generated result in your file manager, so the action matches what Chef actually does.
- The generated app run command now stays usable when the selected project path contains spaces.
