# PR #546

Chef can now keep loading the selected Thread and its messages in Simple Mode even when the browser blocks access to site storage. This prevents a storage permission problem from breaking conversation refresh or the normal task flow.
