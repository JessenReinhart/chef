# 2026-09-07

- #468: Repeated **Copy run command** clicks for the same result now share one clipboard operation, so the UI cannot race into contradictory copy feedback. Retries and newer result versions still work independently.
