# PR #648

When a Mission fails, **Ask Chef to fix it** now carries both the failure reason and the last useful worker activity into the follow-up when they provide different clues. Duplicate or noisy terminal output is cleaned up and bounded so the recovery prompt stays readable.
