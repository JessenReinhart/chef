# PR #552

Chef no longer offers **Show result** for network-style paths that cannot be safely opened as files inside the selected project. Normal project-relative paths, Windows drive paths, and valid `file:` locations keep working as before.
