# PR #550

Simple Mode now stops showing a Mission as **Verifying** when one of that Mission’s own tasks has already failed or become blocked. This keeps failure and recovery feedback truthful instead of making work look healthier than it is.
