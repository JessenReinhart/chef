# PR #501

Simple Mode now restores its working heartbeat after an approval when the same worker task resumes producing output, so long-running work no longer appears to go quiet after approval recovery.
